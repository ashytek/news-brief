@AGENTS.md

# NewsBrief — Real Project Home
**Last updated:** 25 July 2026

## What this is
NewsBrief: Ash's personal news briefing app. Next.js, deployed to Netlify (`netlify.toml` in this folder). Git remote: `ashytek/news-brief`.
This is the live project folder — a separate copy at `~/Desktop/Claude/News App/` is legacy notes only and holds no app code; don't confuse the two.

## Architecture (v2, since July 2026)
- **Transcripts**: Apify actor `codepoetry~youtube-transcript-ai-scraper` is primary (~$0.001/video). Language codes must be bare ISO (`en`, `hi`) — the actor rejects `en-GB`/`en-US`. Local fallback chain (yt-dlp → timedtext → capped AssemblyAI) retained for resilience.
- **Scheduling**: GitHub Actions cron, `.github/workflows/news-pipeline.yml`, 4×/day (03:17/09:17/15:17/21:17 UTC). Ash's Mac is retired from pipeline duty (launchd plist disabled, reversible).
- **Sources**: Supabase `sources` table — not a static config file.
- **Trust layer**: reader header shows pipeline health ("Checked Xh ago" / stale / "Pipeline down"). GitHub emails on workflow failure.
- Full history and rationale: `memory/news-app-v2-architecture.md` in the News-App project memory.

## Reliability follow-ups (14 July 2026)
- Docs were stale (root `README.md` was create-next-app boilerplate pointing at Vercel; `pipeline/README.md` described the retired local-Mac workflow and never mentioned Apify) — both rewritten to match v2. `pipeline/.env.example` was also silently gitignored by the blanket `.env*` rule (never committed) — now tracked, with `APIFY_TOKEN` added.
- Deleted `pipeline/cookies.txt` — unused on any active path (production has no cookie file configured) but was a stale, unencrypted dump of Ash's real browser session cookies. Pure cleanup, no functional change.
- Added a same-run transcript source-mix alert (`pipeline/get_transcripts.py` + `run_pipeline.py`): if Apify (primary) produces zero transcripts in a run while the fragile local fallback chain succeeds, pings the healthcheck `/fail` endpoint immediately instead of relying on the 3-run/~18h zero-story-streak check. **Needs `alter table pipeline_runs add column if not exists fetcher_mix jsonb;` run in Supabase** for the historical mix to persist — the alert itself works without it.
- Diagnosed transcript-layer fragility: the April–June reactive-fix churn (PoToken blocks, 429 handling, error misclassification) was structural to DIY YouTube scraping; the July Apify migration is a genuine fix, not another patch. Residual risk is silent primary-path degradation, which the alert above now covers. Remaining known gaps: single dependency on one third-party Apify actor (`codepoetry~youtube-transcript-ai-scraper`, no fallback actor), hardcoded `["en","hi"]` language list in the Apify request (adding a source in another language needs a code change), and healthchecks.io still isn't set up (so the `/fail` pings above currently have nowhere to land).

## Reliability follow-ups (19 July 2026) — batch fix of the ranked list from the 14 July session
Code changes (built + deployed to git working tree, `npm run build` passes clean):
- **RLS migrations written** (not yet applied — no direct Postgres/Supabase-CLI access from this environment, so these need to be run manually in the Supabase SQL editor): `supabase/migrations/rls_core_tables.sql` (enables RLS + adds `authenticated`-only policies for `stories`, `videos`, `clusters`, `sources`, `read_items`, `engagement`, `muted_topics`, `pipeline_runs`, `topic_keywords`), `supabase/migrations/fix_ranking_weights_security_definer.sql` (closes the horizontal-privilege-escalation hole in `adjust_source_weight`/`adjust_topic_weight`), `supabase/migrations/pipeline_runs_fetcher_mix.sql` (the `fetcher_mix jsonb` column from the 14 July alert work), `supabase/migrations/db_size_alert.sql` (new `get_database_size_mb()` RPC). **Update (20 July):** the original version of the security-definer fix checked `auth.uid() IS NULL` to exempt the service role — but `auth.uid()` is NULL for the `anon` role too, so anon callers slipped through unchecked. Fixed to check `auth.role() = 'service_role'` instead; see the 20 July section below.
- **DB size alert wired into `run_pipeline.py`**: warns + pings healthcheck-fail at 400MB (Supabase free tier caps at 500MB) using the new RPC above. Alert only — no pruning/deletion implemented, that's still a manual decision if the app outgrows free tier.
- **`pipeline/requirements.txt` pinned to exact installed versions** (was all `>=`); dropped `tenacity` (never imported anywhere) and `schedule` (only used by a continuous-loop mode in `run_pipeline.py` that GitHub Actions never exercises — always calls `--once`). Removing `schedule` required also deleting that dead loop + the `INTERVAL_MINUTES` constant + stale module docstring, since the import was unconditional and would have broken every run otherwise.
- **`cluster.get_embedding` now retries with exponential backoff** on 429/5xx/timeout (was raising immediately, which would kill the whole pipeline run mid-loop over one transient error) — matches the manual-retry style already used in `llm.py`.
- **`app/api/search/route.ts` text-fallback search now strips PostgREST/ilike metacharacters** (`, ( ) % _`) from query words before building the `.or()` filter — previously a search term containing those could break or manipulate the filter grammar.
- **`ReaderClient.tsx` + `TodayFeed.tsx` now distinguish "failed to load" from "genuinely no stories"**: a new `loadError` state (set when either Supabase query in `loadContent` returns an `.error`) renders a distinct red error state with a retry button, instead of silently falling through to "All caught up" — which would have masked an RLS misconfiguration or any other query failure as normal empty state.
- Deleted the dead `ANTHROPIC_API_KEY` from `.env.local`, `pipeline/.env`, `pipeline/.env.example` (Gemini is the only LLM in use; confirmed via grep, only a stale comment referenced it).
- Spawned a follow-up task (not fixed here, out of scope of the ranked list): `app/search/SearchClient.tsx` writes like/dislike engagement to a table named `engagement_events`, while `ReaderClient.tsx` and `pipeline/update_weights.py` both use `engagement` — likely means search-page engagement signals are silently lost and never reach the nightly ranking job.

**Still needs Ash to do, can't be done from this environment:**
- Run the four new `supabase/migrations/*.sql` files in the Supabase SQL editor (no direct Postgres connection or Supabase CLI available here).
- Sign up at healthchecks.io and set `HEALTHCHECK_URL` as a GitHub Actions secret in `ashytek/news-brief` — the alerting code (zero-story-streak, fetcher-mix degradation, DB-size, workflow-crash) all already exists and pings this URL, it just has nowhere to land yet.
- **Rotate exposed credentials**: mid-session, a `grep`/`Read` accidentally printed live secrets into chat — Supabase service-role key, two different Google API keys (`.env.local` vs `pipeline/.env` had different values — worth checking why), YouTube Data API key, AssemblyAI key, Apify token, and the already-dead Anthropic key. All should be rotated.

## Code review + fix batch (20 July 2026)
Ran a 3-way review (frontend / pipeline / PWA-infra) then fixed nearly everything found, one item at a time. `npm run build` and `python3 -m py_compile` on every pipeline file pass clean after every batch. Deploy not yet run — build-verified only.

**Android "opens as a browser tab, not fullscreen" — root-caused and fixed.** Two causes: (1) `proxy.ts`'s matcher routed `/manifest.json` and icon files through the auth gate; browsers fetch the manifest without cookies, so the proxy saw "signed out" and 307'd it to `/auth`, and Chrome got an HTML login page where it expected JSON — installability failed outright, and `beforeinstallprompt` never fired. (2) The manifest's only icon was the leftover Next.js template `globe.svg` — no real PNGs. Fixed: matcher now excludes `manifest.json`/`icons/`/static asset extensions; generated real violet-gradient PNG icons (`public/icons/icon-192.png`, `icon-512.png`, maskable variants, `apple-touch-icon.png`) via a one-off Pillow script; manifest and `app/layout.tsx` updated to reference them. Verified in the Browser pane: `/manifest.json` now serves as JSON (not redirected), icons load and render correctly, `/reader` still correctly gates unauthenticated users. **Ash: remove and re-add the home-screen shortcut after this deploys** — Chrome needs a revisit to mint a proper WebAPK.

**Dwell-tracking bug (was silently mass-marking stories read).** `SoloCard`/`ClusteredCard` measured dwell time from component *mount* to *unmount*, not actual visibility — so switching tabs or pulling to refresh (which unmounts every card at once) fired "dwelled 20s+" for cards that were merely rendered, never scrolled into view, silently marking them read and emptying the unread feed. Replaced with `lib/useDwellVisibility.ts`, an `IntersectionObserver`-based hook (50% visibility threshold) shared by both card components. **Could not behaviorally verify in the Browser pane** — confirmed via direct test that `IntersectionObserver` callbacks don't fire at all in this automated preview environment (even a bare manually-constructed observer never fired), which looks like an environment limitation (no real paint/compositor loop), not a code defect. Verified structurally via `npm run build`/typecheck only. **Ash: do a quick real-device sanity check** — scroll past a few cards without pausing, refresh, confirm only stories you actually paused on read as read.

Other fixes, by theme:
- **Security definer hole** (see note above): `adjust_source_weight`/`adjust_topic_weight` now check `auth.role() = 'service_role'` instead of `auth.uid() IS NULL`, which also exempted `anon`. Added `REVOKE EXECUTE ... FROM anon` on both functions and on `get_database_size_mb()` as defense in depth.
- **Open self-signup**: `app/auth/page.tsx` now passes `shouldCreateUser: false` to `signInWithOtp` — this is a single-user app, magic-link sign-in shouldn't let a stranger who finds the URL create an account. **Ash: also confirm "Allow new user signups" is off in the Supabase Auth dashboard** — can't be verified or changed from this environment.
- **Healthcheck alerts were self-cancelling**: all three soft-fail pings (zero-story streak, Apify degradation, DB size) were immediately followed by an unconditional success ping at end-of-run, flipping the check back to "up" seconds later. Added a `soft_failed` flag that skips the success ping when any alert fired.
- **`engagement_events` vs `engagement`**: `app/search/SearchClient.tsx` was writing engagement to a table (`engagement_events`) that has no migration anywhere in the repo — likely meant every search-page like/dislike was silently lost. Changed to `engagement`, matching `ReaderClient.tsx`/`update_weights.py`. Also fixed `SearchClient`'s `markRead`, which used the same `upsert(onConflict)` pattern the codebase's own comments elsewhere document as silently losing writes — switched to plain insert tolerating `23505`.
- **`read_items` unbounded fetch**: all three fetch sites (`ReaderClient`, `ArchiveClient`, `SearchClient`) now `.order('read_at', {ascending:false}).limit(2000)` — Supabase enforces a server-side row cap regardless of `.limit()`, and an unordered query past that cap returned a nondeterministic subset; ordering makes the truncation predictable (oldest reads drop off, not a random slice). **Ash: for a complete fix, consider raising the "Max Rows" API setting in Supabase project settings** — not changeable from this environment.
- **Pipeline resilience**: added a recovery pass (`recover_missing_stories` in `run_pipeline.py`) for videos whose transcript fetched but summarisation failed (e.g. Gemini quota mid-run) — previously lost forever, nothing else revisited them; added per-story exception isolation in the embed/cluster loops (one poison story no longer wedges every future run at the same point); moved the daily-jobs marker from a local file (never persists across GitHub Actions' fresh checkouts — was silently running weight updates ~4× the intended rate) to a new `pipeline_runs.daily_jobs_ran_at` column; fixed retried shorts getting stuck at `transcript_status='failed'` forever (re-fetched, re-billed every run) by always persisting the retry's fresh status; article-scrape failures now return `failed` instead of falling back to title-only text that Gemini would hallucinate a full story from; cluster re-synthesis now actually triggers when a story joins an existing cluster (nothing bumped `last_updated_at` before); consecutive-failure batch aborts now persist a `failed` row for the aborting item and everything still unprocessed, instead of silently dropping them from the retry queue; replaced a `random.uniform` monkey-patch (which also stretched unrelated backoff jitter in `llm.py`/`cluster.py` for the whole retry batch, and did nothing on the Apify path anyway) with an explicit parameter; empty/blocked Gemini responses now count as failures in the usage metrics; added a 60-item-per-run cap so unbounded discovery can't run past the 45-minute workflow timeout.
- **Frontend correctness**: fixed a race condition where rapid tab-switching could show the wrong category's stories (request-token guard in `loadContent`); fixed likes/dislikes only adjusting source weights when the story happened to be in `soloStories` — Today-tab and clustered-story engagement silently never adjusted weights; fixed muted topics only being enforced on category tabs (Today ignored mutes entirely); fixed unread-count drift from what's actually visible (muted/inactive-source items were counted but not shown, so "mark all read" could leave a nonzero badge); batched `markAllVisibleRead` into one insert instead of up to ~200 concurrent ones; added abort-controller + error state to search (stale slow responses could overwrite faster new ones; network failures showed stale results with no indication anything failed); added error-checked, revert-on-failure writes to `TopicsPanel`; fixed Archive's date math using UTC boundaries instead of local time (misbucketed stories published 00:00–01:00 BST); scoped the Topics badge to last-7-days instead of an ever-growing all-time count permanently pinned at 99+; small bugs — `TsLink` dropped `t=0s` links (falsy check on timestamp 0), `formatTime` didn't floor fractional seconds, a no-op `dynamic = 'force-dynamic'` export in a client component (confirmed via build output — `/auth` still renders static either way).
- **Infra**: `topic_keywords` added to the RLS migration (had full browser CRUD, no policy); added basic security headers to `netlify.toml` (X-Frame-Options, nosniff, HSTS, referrer-policy — deliberately no CSP yet, needs careful tuning against Supabase/YouTube/Google origins first); added `permissions: contents: read` to the GitHub Actions workflow.

**Known remaining gap, deliberately not fixed:** `EngagementBar`'s liked/disliked/muted button state is local component state that resets on remount (page refresh) — a user can re-like the same story after a refresh. Low impact (weight nudge is clamped 0.5–1.5, duplicate `engagement` rows have limited downstream effect) but a real fix means threading persisted like/dislike state down through several components; left as a known low-priority item rather than a rushed late-session change.

**Still needs Ash to do:**
- Run all five `supabase/migrations/*.sql` files in the Supabase SQL editor (still no direct Postgres/Supabase-CLI access from this environment) — `rls_core_tables.sql`, `fix_ranking_weights_security_definer.sql`, `pipeline_runs_fetcher_mix.sql`, `pipeline_runs_daily_jobs_marker.sql`, `db_size_alert.sql`.
- Confirm "Allow new user signups" is disabled in the Supabase Auth dashboard.
- Remove and re-add the Android home-screen shortcut after this deploys.
- Everything from the 19 July list is still outstanding too: healthchecks.io signup + `HEALTHCHECK_URL` secret, and rotating the credentials exposed mid-session (that session's Supabase/Google/YouTube/AssemblyAI/Apify keys — separate from this session, no new exposures this time).
- A spawned background task exists to resolve the `engagement_events` question properly (confirm the table doesn't exist / add its own RLS if it does) — superseded by the direct fix above, but the task chip may still be showing.

## Database migrations applied + RLS audit (23 July 2026)
Ash generated a Supabase Management API personal access token so migrations could actually be run from a session, instead of relying on manual copy-paste into the SQL editor (which is how earlier attempts silently failed — see below). Token was used, verified, then deleted from `pipeline/.env` and **should be revoked from the Supabase dashboard** (Account Settings → Access Tokens) since it's no longer needed day-to-day.

**Root cause of "new row violates row-level security policy" on `sources`:** the table already had a pre-existing RLS SELECT policy (`"read sources"`, granted to `{public}` role but gated by `auth.role() = 'authenticated'` in its condition) created at some point directly via the Supabase dashboard UI — not by any file in this repo. That's why reads worked. There was no INSERT policy at all, from any source, which is why the "Add source" form always failed — including after re-running the snippet meant to fix it, because that CREATE POLICY call was never actually reaching the database (unclear why — possibly not fully executed in the SQL editor). Fixed by running the INSERT policy directly via the Management API and confirming via `pg_policies`.

**All 5 pending `supabase/migrations/*.sql` files are now applied**: `rls_core_tables.sql`, `fix_ranking_weights_security_definer.sql`, `pipeline_runs_fetcher_mix.sql`, `pipeline_runs_daily_jobs_marker.sql`, `db_size_alert.sql`. Verified via direct SQL: RLS is enabled on all 9 tables (`stories`, `videos`, `clusters`, `sources`, `read_items`, `engagement`, `muted_topics`, `pipeline_runs`, `topic_keywords`); `pipeline_runs.fetcher_mix`/`daily_jobs_ran_at` columns exist; `adjust_source_weight`/`adjust_topic_weight` have the `service_role` guard.

**Found and fixed a real pre-existing security hole while auditing the applied state**: nearly every table already had a pre-existing dashboard-created policy (all granted to `{public}` role) that predates this repo's migrations entirely. Most were safely gated (`auth.role() = 'authenticated'` or `auth.uid() = user_id` in the condition), so functionally equivalent to the new ones — just redundant. But `topic_keywords`' read policy (`"allow_read_topic_keywords"`) had `qual: true` — a genuinely open read, no auth check at all, reachable by anyone with the public anon key (shipped in client JS), no login required. Dropped it, along with the other now-redundant duplicate policies (having two overlapping policies per table with different names is exactly the kind of drift that makes a future audit miss something real, like this one). Verified with a live anon-role test: `topic_keywords` now correctly returns 0 rows for an unauthenticated request. Every table now has exactly one policy, matching what's in this repo's migration files — single source of truth restored.

Also added a channel via direct insert (service-role key, bypassing the then-broken policy) as an immediate unblock: **India Global Review** (`india_global` category, 24h lookback, `HANDLE:@IndiaGlobalReview` — resolves to a real channel ID on the pipeline's next run).

## Three features: manual trigger, Vantage recap skip, clustering removed (25 July 2026)
Planned via plan-mode (full plan at the time: `/Users/ashmac/.claude/plans/is-there-a-possibility-lively-starlight.md`), reviewed by an independent second opinion (Fable model) before implementation — the review caught two real bugs in the initial design (a healthcheck false-alarm on the skip path, and a polling query that couldn't actually detect a running pipeline run) that were fixed before writing any code. `npm run build` and `python3 -m py_compile` on every touched pipeline file pass clean after every change; a comprehensive grep sweep confirmed no leftover references to anything removed.

**Feature 1 — Manual pipeline trigger.** A "⚡ Run now" control in the reader header (next to the "Checked Xh ago" status, not the icon row — keeps a costly action off the crowded row with a documented overflow history, and visually distinct from the pre-existing, purely-cosmetic "Refresh feed" icon so the two aren't confused). New route `app/api/pipeline/trigger/route.ts` calls GitHub's `workflow_dispatch` API, gated by: auth, a check against GitHub's own queued/in-progress runs (closes the ~2-4 min blind window between a successful dispatch and the pipeline's own `pipeline_runs` row appearing), a 50-minute staleness escape on a stuck `running` row (workflow times out at 45 min), and a 15-minute cooldown. Two-phase client-side polling (15s while waiting for the run to start, up to 10 min; 30-60s once it's running, up to 45 min) watches `pipeline_runs` for a new row by **id**, not timestamp (avoids clock-skew issues) — refreshes the feed and pipeline-health indicator automatically on completion.

Per Ash's explicit request, a scheduled cron run now skips real work entirely (no Apify/Gemini cost) if a **successful or partial** manual run started within the last 5 hours (`pipeline_runs.trigger_source`, new column, set from `github.event_name` via the workflow YAML) — deliberately excludes failed/still-running manual triggers so a crash can't compound into a ~12h silent gap. The skip path pings the healthcheck as **success** before returning (a deliberate skip is a healthy state — the real success ping lives at the very end of `run_once()`, unreachable on an early return, so without this every skip would eventually false-alarm "pipeline down"). Accepted tradeoff of the 5h window: content can go up to ~11h without a fresh pull if a manual trigger lands right before a scheduled slot.

**Feature 2 — Skip Gemini for long Vantage recap videos.** Vantage/Firstpost videos over 20 minutes (`MAX_VANTAGE_RECAP_SECONDS`, `pipeline/config.py`) are usually recap/rehash content per Ash — skipped before any network/Gemini call (`_is_vantage_source()` + duration check in `run_pipeline.py`'s main loop, new `transcript_status = 'skipped_long_recap'`, follows the exact same vocabulary/write-path as the pre-existing `skipped_short`). Known accepted gap: `duration_seconds` is only reliably populated for fresh discovery items, not the retry/recovery queues, so a retried long-recap video won't be caught a second time — rare, low-cost.

**Feature 3 — Story clustering fully disabled.** Multi-source clustering (grouping same-event stories into one `ClusteredCard`) was cramming each contributing source's coverage into a small card — truncated to 3 bullets vs. a solo story's 5+/expandable, richest content behind a click-to-expand toggle, small thumbnails. Every story now renders as its own full card. Also removes an extra Gemini synthesis call per multi-source event (second win for the token-conservation goal behind Feature 2). Pipeline: `cluster.py` still embeds every story (search needs it) but no longer calls the clustering-assignment RPC; `synthesise_ready_clusters()` and the whole `--recluster` CLI mode are gone, along with every function that only existed to support them (`find_or_create_cluster`, `db.py`'s `get_or_create_cluster`/`assign_story_to_cluster`/`update_cluster`/`increment_cluster_story_count`/`get_recent_embeddings`, `summarise.py`'s `synthesise_cluster`). Frontend: `ClusteredCard.tsx` deleted, `Cluster`/`ClusterWithRelations`/`PerspectiveItem` types removed, `lib/ranking.ts` simplified to solos-only (dropped the `story_count * 1.5` ranking bonus clusters used to get), `ReaderClient.tsx`/`TodayFeed.tsx` simplified accordingly. `read_items.cluster_id` and `stories.cluster_id` columns are left in place (harmless, unused going forward) — not worth a migration to drop them.

**Still needs Ash to do:**
- Create a fine-grained GitHub PAT scoped to only `ashytek/news-brief`, Actions permission **Read and write**, and add it to **Netlify's dashboard** as `GITHUB_PIPELINE_PAT` (Site settings → Environment variables — no dashboard access from this environment). Note its expiry date somewhere — fine-grained PATs expire at most 1 year out, and when this one does, the trigger button will start failing with a 502 and a "PAT expired or revoked?" line in the server logs.
- Run `supabase/migrations/pipeline_runs_trigger_source.sql` (safe, additive column).
- Run `supabase/migrations/disable_clustering_wipe.sql` **only after explicitly confirming** — it permanently deletes existing `clusters` rows and their consensus/perspectives text (the underlying `stories` rows and their own summaries/embeddings are untouched).
- After deploying, do **one real deliberate trigger** (this costs real Apify/Gemini money) to verify the full path end-to-end, watching the GitHub Actions tab and the reader header simultaneously. Then at the *next* scheduled cron slot, check the Actions tab — the run should still appear but complete in seconds with a "Skipping scheduled run" log line, confirming the skip logic worked.

## Dwell auto-read timer raised 20s → 120s (3 September 2026)
Ash reported stories disappearing from the feed while he was still reading them. Root cause is a four-step chain, not a single bug: `useDwellVisibility` (threshold 0.5) fires `onDwellEnd` as soon as a card drops below half-visible → `endDwell` (`ReaderClient.tsx`) auto-marks it read if it was on screen past the dwell threshold → `markRead` updates `readIds` optimistically, before the network round-trip → `visibleSolos` filters `readIds` out of the feed, and `showUnreadOnly` defaults to **true**. Net effect: read a card for longer than the threshold, scroll down slightly to continue it, and the card is unmounted under you while everything below jumps up. On the Today tab the card isn't removed but is re-sorted to the bottom by `rankItems` (`ranking.ts`), which looks the same from the reader's seat.

**Changed:** the threshold only, 20 → 120 seconds (`ReaderClient.tsx`, the sole place it's defined). `npm run build` clean; confirmed `a>120` in the emitted chunk.

**Deliberately NOT changed — the root cause is still live.** Ash was offered a ~10-line fix (freeze cards already on screen; let them grey out in place and only drop off on the next explicit refresh, the Feedly/Reeder pattern) and declined it twice in favour of the timer alone. So a story held on screen past 120s still vanishes mid-read, by decision, not oversight — don't "fix" this silently in a later session, and don't re-diagnose it from scratch. Raising the number also inverts which stories break: quick skims are now safe, long-form pieces are the ones still at risk.

**Unconfirmed, worth checking if this resurfaces:** with `threshold: 0.5`, a card taller than ~2× the viewport can never reach a 0.5 intersection ratio, so no threshold crossing ever fires and `onDwellStart` may never run for it — meaning the longest stories might never auto-mark-read at all. Reasoned from the IntersectionObserver spec, not observed in the running app.

## Rules for this folder
- Read the relevant component only before changing code — not the whole repo. Use a subagent for repo-wide reviews.
- Verify changes with `npm run build` / local preview and show evidence before deploying.
- Deploy with `npm run deploy` (`npx netlify-cli deploy --prod --build`).
- Don't touch the disabled launchd plist (`~/Library/LaunchAgents/com.ashmac.newsbrief.plist.disabled`) or re-enable local pipeline scheduling without confirming with Ash — the cloud cron is now the source of truth.
- Never paste `.env.local` contents or API tokens into chat; if a token is ever pasted, flag it for rotation.
